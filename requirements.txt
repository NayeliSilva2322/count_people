# Plugin pack — runtime dependencies only
#
# Editions in this pack: tensorrt, ultralytics, ort
#
# Setup:
#   pip install -U pip wheel
#   # Matched CUDA pair (required — do not use bare pip torch alone):
#   pip install torch==2.12.1 torchvision==0.27.1 \
#     --index-url https://download.pytorch.org/whl/cu132
#   # CPU-only alternative: pip install torch==2.12.1 torchvision==0.27.1
#   pip install -r requirements.txt
#   pip install -e .

numpy>=1.24
opencv-python>=4.8
Pillow>=10.0
PyYAML>=6.0
lap>=0.4
scipy>=1.10
torch==2.12.1
torchvision==0.27.1
ultralytics>=8.2
onnxruntime-gpu>=1.17; sys_platform == 'linux'
onnxruntime>=1.17; sys_platform == 'win32'
onnxruntime>=1.17; sys_platform == 'darwin'
